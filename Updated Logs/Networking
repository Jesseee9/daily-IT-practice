# Week 1 — Monday Network Session

**Tool:** Cisco Packet Tracer  
**Date:** 2026-04-06  

## Tasks Completed
- Router, switch, and 3 PCs added from scratch.
- Router interface GigabitEthernet0/0 brought up (`no shutdown`).
- Physical link to switch verified (green cable).
- Router IP 192.168.1.1 assigned as default gateway.
- DHCP pool partially configured; router IP excluded.
- Verified interface status with `show ip interface brief`.

## Key Concepts
- Interface status and physical connectivity indicators.
- Router as DHCP server and default gateway.
- Importance of CLI accuracy (`no shutdown` vs `shutdown`).
- Stepwise troubleshooting.

## Lessons Learned
- Must bring interfaces up before testing DHCP.
- Correct CLI commands prevent misconfiguration.
- Verification commands confirm setup.
- Network setup requires logical and physical checks.

## Next Steps
- Complete DHCP pool configuration.
- Ensure PCs receive IPs automatically.
- Test connectivity between all devices.
